<% if current_user.user_role_id == 1 %>
  <% if media_callouts %>
  <p class="count"><%="#{media_callouts.length} story suggestions match the selected interests." %></p>
  <% end %>
<% else %>
  <% if media_callouts %>
  <p class="count"><%="#{media_callouts.length} callouts match the selected interests." %></p>
  <% end %>
<% end %>